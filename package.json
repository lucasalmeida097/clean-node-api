{
  "name": "clean-node-api",
  "version": "1.0.0",
  "description": "Building a Rest API in NodeJS using TDD, Clean Architecture and Design Patterns",
  "main": "index.js",
  "scripts": {
    "test": "jest --watchAll --silent --colors --noStackTrace",
    "test:staged": "jest --passWithNoTests --findRelatedTests --silent --noStackTrace",
    "test:ci": "jest --coverage --noStackTrace --silent",
    "prepare": "husky install"
  },
  "standard": {
    "env": ["jest"]
  },
  "author": "Lucas Almeida Bezerra'",
  "license": "ISC",
  "devDependencies": {
    "husky": "^8.0.0",
    "jest": "^29.7.0",
    "lint-staged": "^15.2.0",
    "standard": "^17.1.0"
  }
}
